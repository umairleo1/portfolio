import { animationConfig } from '@/config/animations';

export const staggerContainer = animationConfig.stagger.container;
export const staggerItem = animationConfig.stagger.item;

// Re-export other animations from config for convenience
export * from '@/config/animations';
